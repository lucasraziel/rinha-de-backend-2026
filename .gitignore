/target
**/*.rs.bk
Cargo.lock.bak

# Big artifacts regenerated by `crates/prep` from upstream — never committed.
# CI runs prep before `docker build`. Locally: ./scripts/fetch-resources.sh && cargo run --release -p prep
/resources/references.json.gz
/data/

# Local k6 output
/bench/results.json

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
